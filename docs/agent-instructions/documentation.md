# Documentation

- Keep `AGENTS.md` high-level and easy to scan.
- Put deeper agent/process guidance in `docs/agent-instructions/`.
- Use `AGENTS.md` to point future agents to the relevant detailed guidance files instead of expanding the root file too much.
- When summarizing work, distinguish the main architectural change from smaller cleanup refactors.
