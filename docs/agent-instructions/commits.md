# Commits

- Match the existing repo style before suggesting a commit message.
- Prefer simple sentence-style commit messages without conventional-commit prefixes.
- Start with an imperative verb such as `Add`, `Update`, `Refactor`, `Replace`, or `Use`.
- For larger changes, describe the user-visible or architectural effect rather than only the helper or cleanup that happened along the way.
