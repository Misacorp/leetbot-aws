# Testing

- Do not add new tests unless relevant existing tests already cover the same behavior area, or the user explicitly asks for a test.
- For infra-heavy refactors, do not invent new test coverage just to satisfy process.
- If a helper test is explicitly requested, keep it focused and colocated with the helper.
